The set of builtins will be constructed using only those tokens 
(space or new-line separated) in this file that start with a dollar sign.

All the rest is considered a comment and silently ignored.

$url                Get the current url, or set it (ie, go to the selected page)

$title   READONLY   Get the title of the page. Setting it has no effect;
$millis  READONLY   Time in milliseconds since 1/1/1970. Setting it has no effect.
$count   READONLY   Counter inside a running loop, or 0 if outside.