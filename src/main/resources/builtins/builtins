
    /**
     * *********************************
     * Default built-in functions * * They will be incorporated as-is * in the
     * generated scrapper file.*
     *
     * Note that the list of recognized builtins is defined in the resources 
     * file "builtins.list", in a whitespace or nl separated format.
     **********************************
     */
// Note that the list of recognized builtins is defined in the resources 
// file "builtins.list"


    public String BUILTIN_title_get() {
        return wd.getTitle();
    }

    public void BUILTIN_title_set(String inputString) {
        throw new UnsupportedOperationException("Not supported yet.");
    }

    public String BUILTIN_url_get() {
        return wd.getCurrentUrl();
    }

    public void BUILTIN_url_set(String inputString) {
        wd.get(inputString);
    }

    /* basic emit implementation - override as needed */
    public void BUILTIN_emit(Map<String, String> m) {
        System.out.printf("%n%s%n",m.toString());
    }

    /**
     * Default main will open a single remote driver, run and emit results, then
     * close driver.
     *
     * @param args
     */
    public static void main(String[] args) {
        WebDriver wd = null;
        try {
            wd = new RemoteWebDriver(DesiredCapabilities.firefox());
            new AutoScrapper().scrap(wd);
            wd.close();
        } finally {
            if (wd != null) {
                wd.close();
            }
        }

    }

    /**
     * ********** end of built-in declarations *********
     */

